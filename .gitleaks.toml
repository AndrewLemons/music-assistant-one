[extend]
useDefault = true

[[allowlists]]
description = "Upstream Swift type declaration in patch context, not key material"
condition = "AND"
paths = ['''(^|/)patches/sendspinkit-release\.patch$''']
regexTarget = "secret"
regexes = ['''^Curve25519\.KeyAgreement\.PrivateKey$''']
